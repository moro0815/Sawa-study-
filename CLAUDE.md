# 沙和ナビ

## これは何か

中学1年から大学受験まで、**6年間ひとりの子に伴走する**AI学習システム。
不特定多数向けの製品ではなく、**沙和さん(12歳・中1)専用**として作っている。

## 使う人

| | 誰 | どのくらい使うか | 前提にしてよいリテラシー |
|---|---|---|---|
| **本人** | 沙和さん(中1・女子) | ほぼ毎日、5〜30分 | スマホの操作はできる。**「API」「キー」「トークン」「更新プログラム」などの技術語はまったく通じない**。ローマ字入力は遅い |
| **保護者** | 父(このリポジトリの持ち主) | 週1回ほど、設定と見守り | エックスサーバーのファイル管理・ZIPアップロードまでは操作できる。**コードは書かない**。ターミナルは使わない |
| 将来 | 学校・塾の先生に見せる可能性 | 面談時など | 教育関係者。**研究の引用が甘いと信用を失う** |

**沙和さんは飽きやすく、わからないことを人に聞くのが少し苦手。** 夢は獣医。
この2つが、機能を足すかどうかの判断基準になっている。

## 技術スタック

**バージョンより「使っていないもの」のほうが重要。**

- HTML / CSS / **素のJavaScript**(フレームワークなし、モジュールなし、`<script>` を順番に読むだけ)
- PHP 8.1+ — `api/backup.php` **1ファイルだけ**(`str_contains` と `never` 戻り値を使用)
- ブラウザAPI:Service Worker / IndexedDB / Web Speech / MediaRecorder / Pointer Events / Web Share
- 配置先:エックスサーバー(Apache)。**ZIPを解凍して `public_html` に上書きするだけで更新**

**ビルド工程は無い。npm も Node も要らない。** これは制約ではなく、この運用を成立させている条件。

## 絶対に守ること

### 秘密の取り扱い

- **APIキーをコミットしない。** キーは端末の `localStorage` のみ
- **端末の外へ出るものに `apiKeys` と `srvToken` を入れない。**
  対象は4つ:ファイル書き出し / サーバーのひかえ / 保護者レポート(`share.html`) / 復元用リンク
  → 必ず `backupPayload(false, …)` を使う。`true` は端末内のひかえ専用
  → **例外は復元用リンクだけ**(`srvToken` を含む。用途上そうでないと機能しない。APIキーは含めない)
- 提供元のエラー文など、秘密が混ざりうる文字列は**保存・表示の前に `scrubSecrets()` を通す**
- リポジトリは **Private のまま**。`noindex` と `robots.txt` を外さない

### 子どもの画面に出してよい言葉

- **「API」「キー」「トークン」「認証」「401」を沙和さんの画面に出さない。**
  12歳に伝えて意味があるのは「おうちの人に伝えてね」だけ
- 保護者タブには**逆に具体的に**出す(どのプロバイダか、何日前に切れたか)
- **休んだことを責めない。** 赤い警告も、連続日数の表示も出さない
- 終了時に**正答率を出さない**。「できるようになったこと」を言葉で出す

### データ

- **生データ(`js/rawlog.js`)は消さない。** 分析データは生データから作り直せる状態を保つ
  → `rebuildFromRaw()` が通らなくなったら、それは設計の後退
- **CSP を緩めない。** `script-src` に `'unsafe-inline'` / `'unsafe-eval'` を足さない
  通信先を増やすときは `index.html` の `connect-src` に1行足すだけ(**ここ1か所**)

### 研究の引用

- **効果量を汎用の数字として書かない。** 「テスト成績が2倍」ではなく、研究名と実際の値と条件を書く
- 実装していないものを「実装した」と書かない(例:本家FSRSではないので「FSRS-lite」)
- **できないことを、できるふりにしない**(例:書き順は画像から判定できないので判定しない)

### リポジトリ

- push は `claude/ai-tutor-system-kkqm3g` **のみ**。`git push -u origin <branch>` を使う
- `gh` CLI は無い。GitHub操作は MCP ツール経由

## 命名・実装の流儀

### 日付 — ここは一度事故った

- **日付は必ず `todayISO()`**(現地時間基準)。`today()` はその別名
- **`new Date().toISOString().slice(0,10)` を直接書かない。**
  UTC基準になり、**日本時間の 0〜9時に1日ずれる**。記録は「昨日」に入り判定は「今日」を見る、という食い違いが実際に起きた

### 識別子

- 概念ID:`curriculum.js` の `CONCEPT_MAP` のキー(例 `m1-jojo`)。210件
- 書く練習の項目ID:`kanji:険` / `spell:believe`(`writeItemId()` が作る)
- 誤答原因:`ERROR_CAUSES` の**12種類だけ**。カルテも1問ごとの記録も**同じ分類を使う**
  → 別の分類を作ると、どちらの記録も突き合わせられなくなる

### 画面

- `innerHTML` に入れる文字列は**必ず `esc()`** を通す
- AI向けの `**強調**` を画面にそのまま出さない(`plainMove()` のように剥がす)
- 押すところは**指の端末で最低42px**(`@media (pointer: coarse)`)
- **入力欄を16px未満にしない。** iOS が勝手に拡大する(iPad で実際に起きた)

### バージョン

- 変更を出すときは `js/app.js` の `APP_VERSION` と `sw.js` の `CACHE` を**両方**上げる
  → 片方だけだと、古いファイルがキャッシュに残り続ける

## 触ると壊れやすい場所

**すべて実際に壊した場所。** 同じ壊し方を繰り返さないために書いている。

| 場所 | 何が起きたか |
|---|---|
| **クラス名の衝突** | 受験タブの節目一覧が `.ms-*`、ホームのミッションカードも `.ms-*` だった。`.ms-body` が共通で、片方を触るともう片方が崩れる。→ ミッション側を `.mi-*` に変更済み |
| **クラス名だけで拾うハンドラ** | `$$(".conf-btn")` が、書く練習の確信度ボタンまで巻き込んで上書きし、壊していた。→ `#confPanel` 内に限定。**セレクタは必ずスコープを付ける** |
| **`backupPayload(secrets)`** | `true` にすると鍵が入る。書き出し系で `true` を使うと**鍵が端末の外へ出る**。実際に1度そうなった |
| **書き出し処理の二重実装** | 同じことをする関数が2つあり、片方だけ `secrets=true` だった。**書き出しは `shareBackup()` の1本だけ** |
| **`rawOpen()` の IDBRequest** | 完了前に `.result` を読むと `InvalidStateError`。それがタイマー内だと Promise が永久に解決せず、**生データが1件も保存されなくなる**(画面には何も出ない)。→ `settled` フラグで先に決まったものだけ採用 |
| **人格プロンプト** | Luke の説明を全人格に渡していたため、ふくろうのナギが「自分は犬」と答えた。→ 人格ごとに文面を変え、**先頭と末尾の両方**に「あなたは誰か」を置く |
| **SVG のグラデーション** | `radialGradient` は既定が `objectBoundingBox` で、図形ごとに別々に陰影がつく。Luke の毛が数珠つなぎのビーズに見えた。→ `gradientUnits="userSpaceOnUse"` |
| **`api/backup.php` の init** | 最初に叩いた相手が合言葉を持っていける形だった。→ PIN 必須に。**PINが無ければ発行しない**(閉じる側に倒す) |
| **AIとの履歴** | `tool_result` には直前に対応する `tool_use` が必要。崩れると 400。→ `repairPairs()` を通す |
| **例文に答えが出る** | つづり練習で `I know his name.` と答えが見えていた。→ `maskAnswer()` で語尾変化ごと伏せる |

## 作業の進め方

- **大きな変更の前に、まず計画を出して確認を取る**
- 機能ひとつ終わるごとにコミット。メッセージは**日本語**で、**何をなぜ変えたか**
  → 「なぜ」は特に大事。上の表は全部コミットメッセージから拾える状態にしておく
- 区切りがついたら `docs/DECISIONS.md` に決定事項を追記する
- 変更したら Playwright の一式(`scratchpad/*.js`)を通す。**「JSエラー:(なし)」を確認してから push**
- **推測で「直りました」と言わない。** 実際に動かした出力を貼る
- 指摘を受けたら、まず**自分のコードを読んで事実確認**してから答える

## 現在の状況

**完了**

- 知識の系譜グラフ(全教科210概念)・前提遡行診断・確信度4象限・簡易記憶モデル
- 誤答原因12分類 / 転移レベル5段階(Lukeへの説明でMastery判定)/ 学習カルテ / 実測パターン
- 3人格(ミミ先生・Luke・ナギ)・相棒犬Luke(写真差し替え可)
- 英語5層(発音・会話・文法・語彙・読み書き・受験)
- 書く学習(漢字31字 + つづり61語、手書きキャンバス)
- 宿題取り込み(アプリ内カメラ / スキャン)・保護者タブ・共有レポート
- 3秒スタート / 5分モード / 復帰UX / 終わり方
- 生データ3層構造(IndexedDB、消さない、作り直せる)
- CSP・AIの使用期限・記録を失わないための守り5枚・バックアップ初期化のPIN化
- iPhone / iPad 実測対応(320〜1024px)

**進行中**

- なし(v35 時点で指摘対応はすべて反映済み)

**未着手・検討中**

- `main` ブランチが無いため、**プルリクエストが作れない**。ZIPを落とすときもブランチ選択が必要
  → 保護者の判断待ち
- APIキーをサーバー側プロキシに移す案(安全性は上がるが、**PHPだけ・Node不要・ZIP上書き**を壊す)
  → 現状は CSP で塞ぐほうが割に合うと判断。保留
- 本家FSRSのパラメータ最適化(生データが十分たまってから)
- 漢字・つづりの種を増やす(いまは「書けない語」に絞った最小限)
